procedure main() returns integer {
	var out = std_output();
	var inp = std_input();
	integer x = 5;

	if (x > 6)
		x -> out;
	else if (x > 5)
		x + 1 -> out;
	else if (x > 4)
		x + 2 -> out;

	'\n' -> out;


	return 0;
}
